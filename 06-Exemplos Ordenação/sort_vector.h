#ifndef GUARD_SORT_VECTOR
#define GUARD_SORT_VECTOR

#include <vector>

void bubblesort(std::vector<int>&);
void selectionsort(std::vector<int> &);
void insertionsort(std::vector<int> &);
void quicksort(std::vector<int>&, int, int);

#endif
